<h1>Burgers!</h1>

<h2>Burgers that need to be eaten!</h2>

<ul>
  {{#each burgers}}
    {{#unless eaten}}
      {{> burgers/burger-block eat=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that I have already eaten!</h2>

<ul>
  {{#each burgers}}
    {{#if eaten}}
      {{> burgers/burger-block eat=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="Hungry">Hungry?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Eat Now!<br>
    <input type="radio" name="sleepy" value="0"> Eat Later
  </div>

  <button type="submit">Add Burger</button>
</form>
